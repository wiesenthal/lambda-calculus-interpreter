{
  "name": "lambda-calculus-interpreter",
  "version": "1.0.0",
  "description": "A pure TypeScript lambda calculus interpreter",
  "main": "dist/index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "dev": "ts-node src/repl.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["lambda calculus", "interpreter", "typescript"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.9.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
